{
  "name": "mgen1",
  "version": "1.0.0",
  "description": "Frappe application for mgen1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "production": "FRAPPE_ENV=production node build.js --production",
    "dev": "FRAPPE_ENV=development node build.js --watch"
  },
  "keywords": [
    "frappe"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "frappe-ui": "^0.0.1"
  }
}
